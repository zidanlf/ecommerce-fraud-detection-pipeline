name: 'fraud_analytics'
version: '1.0.0'
config-version: 2
profile: 'fraud_analytics' # Refers to profiles.yml entry
model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  fraud_analytics:
    staging:
      +materialized: view
    core:
      +materialized: table
    marts:
      +materialized: table